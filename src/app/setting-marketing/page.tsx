import { Metadata } from "next";
import PlaceholderContent from "@/components/PlaceholderContent";

export const metadata: Metadata = {
  title: "Setting Marketing | PaNen",
  description: "PaNen Setting Marketing",
};

const SettingMarketingPage = () => {
  return <PlaceholderContent text="Setting Marketing Page" />;
};

export default SettingMarketingPage;
